@model VeterinaryPassport.Models.Passport

@{
    ViewBag.Title = "Паспорт";
}

<div class="details-main">
    <div>
        <h1>Питомец</h1>
        <p><b>Кличка:</b> @Model.Pet.Name</p>
        <p><b>Вид:</b> @Model.Pet.Kind</p>
        <p><b>Пол:</b> @Model.Pet.Sex</p>
        <p><b>Порода:</b> @Model.Pet.Breed</p>
        <p><b>Дата рождения:</b> @Model.Pet.DOB.ToString("dd/MM/yyyy")</p>
        <h1>Владелец</h1>
        <p><b>Владелец:</b>  @Model.Owner.Surname @Model.Owner.Name @Model.Owner.Patronymic</p>
        <p><b>Телефон:</b> @Model.Owner.PhoneNumber</p>
        <p><b>Адрес:</b> @Model.Owner.Address</p>
    </div>
      <a class="btn btn-outline-secondary" asp-controller="Vaccine" asp-action="VaccineRead" asp-route-id="@Model.Id">Прививки</a>
    @*<div class="details-main-right">
        <a class="details-a" asp-controller="Disease" asp-action="Index" asp-route-id="@Model.Id">
            <div class="frame">
                <div class="images-disease"></div>
                <p class="frame-title">Заболевания</p>
                <p>Здесь расположены перенесенные заболевания пациента</p>
            </div>
        </a>
        <a class="details-a" asp-controller="Anthropological" asp-action="Index" asp-route-id="@Model.Id">
            <div class="frame">
                <div class="images-anthropological"></div>
                <p class="frame-title">Антропологические измерения</p>
                <p>Здесь находятся измерения роста и веса пациента</p>
            </div>
        </a>
        <a class="details-a" asp-controller="Vaccine" asp-action="Index" asp-route-id="@Model.Id">
            <div class="frame">
                <div class="images-vaccine"></div>
                <p class="frame-title">Прививки</p>
                <p>Здесь записываются все прививки, которые были сделаны пациенту</p>
            </div>
        </a>*@
</div>
</div>